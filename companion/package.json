{
  "name": "cursor-mobile-companion",
  "version": "1.0.0",
  "description": "Desktop companion that indexes local Cursor chats and bridges the iOS app over LAN",
  "main": "dist/main.js",
  "author": "Cursor Mobile Companion",
  "license": "MIT",
  "homepage": "https://github.com/cursor-mobile/cursor-mobile",
  "scripts": {
    "build": "tsc && node scripts/copy-renderer.js",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder",
    "dist:mac": "npm run build && electron-builder --mac dmg --publish never",
    "dist:linux": "npm run build && electron-builder --linux AppImage --publish never",
    "dist:win": "npm run build && electron-builder --win nsis --publish never"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.27",
    "bonjour-service": "^1.3.0",
    "qrcode": "^1.5.4",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/qrcode": "^1.5.5",
    "@types/ws": "^8.18.1",
    "electron": "^37.2.6",
    "electron-builder": "^26.0.12",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
